<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.customer.controller.AccountController"
      spacing="0" styleClass="content-area">

    <!-- Page Header -->
    <HBox styleClass="page-header" alignment="CENTER_LEFT">
        <Label text="🏦 Accounts" styleClass="page-title"/>
    </HBox>

    <!-- Search and Filter Bar -->
    <HBox spacing="15" alignment="CENTER_LEFT" styleClass="toolbar">
        <padding>
            <Insets top="10" right="30" bottom="10" left="30"/>
        </padding>

        <Label text="Search:" styleClass="section-label"/>
        <TextField fx:id="searchField" promptText="Search by account number or customer name..."
                   prefWidth="300" HBox.hgrow="ALWAYS"/>

        <Label text="Type:" styleClass="section-label"/>
        <ComboBox fx:id="typeFilter" prefWidth="130"/>

        <Label text="Status:" styleClass="section-label"/>
        <ComboBox fx:id="statusFilter" prefWidth="130"/>

        <Region HBox.hgrow="ALWAYS"/>

        <Button fx:id="refreshButton" text="🔄" onAction="#handleRefresh"
                styleClass="secondary-button" style="-fx-padding: 10px 15px;"/>
    </HBox>

    <!-- Split Pane for Account Table and History -->
    <SplitPane VBox.vgrow="ALWAYS" orientation="VERTICAL" dividerPositions="0.6" style="-fx-background-color: transparent;">
        <items>
            <!-- Account Table Section -->
            <VBox>
                <padding>
                    <Insets top="10" right="30" bottom="10" left="30"/>
                </padding>

                <StackPane VBox.vgrow="ALWAYS">
                    <TableView fx:id="accountTable" VBox.vgrow="ALWAYS">
                        <columns>
                            <TableColumn fx:id="accountNumberColumn" text="Account Number" prefWidth="150"/>
                            <TableColumn fx:id="customerNameColumn" text="Customer Name" prefWidth="180"/>
                            <TableColumn fx:id="typeColumn" text="Type" prefWidth="100"/>
                            <TableColumn fx:id="balanceColumn" text="Balance" prefWidth="150" style="-fx-alignment: CENTER-RIGHT;"/>
                            <TableColumn fx:id="interestRateColumn" text="Interest Rate" prefWidth="100" style="-fx-alignment: CENTER-RIGHT;"/>
                            <TableColumn fx:id="statusColumn" text="Status" prefWidth="100"/>
                            <TableColumn fx:id="createdDateColumn" text="Created Date" prefWidth="150"/>
                        </columns>
                        <columnResizePolicy>
                            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                        </columnResizePolicy>
                    </TableView>

                    <!-- Loading Overlay -->
                    <StackPane fx:id="loadingOverlay" styleClass="loading-overlay"
                               visible="false" managed="false">
                        <ProgressIndicator styleClass="loading-spinner" maxWidth="50" maxHeight="50"/>
                    </StackPane>
                </StackPane>
            </VBox>

            <!-- Transaction History Section -->
            <VBox>
                <padding>
                    <Insets top="0" right="30" bottom="10" left="30"/>
                </padding>

                <!-- Action Buttons moved here -->
                <HBox spacing="15" alignment="CENTER_LEFT" styleClass="toolbar">
                    <padding>
                        <Insets top="10" right="0" bottom="10" left="0"/>
                    </padding>

                    <Button fx:id="openAccountButton" text="➕ Open" onAction="#handleOpenAccount" styleClass="success-button"/>
                    <Button fx:id="viewDetailsButton" text="👁️ View" onAction="#handleViewDetails" styleClass="primary-button"/>
                    <Button fx:id="freezeUnfreezeButton" text="❄️ Freeze" onAction="#handleFreezeUnfreeze" styleClass="warning-button"/>
                    <Button fx:id="closeAccountButton" text="❌ Close" onAction="#handleCloseAccount" styleClass="danger-button"/>

                    <Region HBox.hgrow="ALWAYS"/>

                    <Button fx:id="depositButton" text="💰 Nạp tiền" onAction="#handleDeposit" styleClass="success-button" style="-fx-background-color: #27ae60;"/>
                    <Button fx:id="withdrawButton" text="💸 Rút tiền" onAction="#handleWithdraw" styleClass="danger-button" style="-fx-background-color: #c0392b;"/>
                    <Button fx:id="transferButton" text="➡️ Chuyển khoản" onAction="#handleTransfer" styleClass="primary-button" style="-fx-background-color: #8e44ad;"/>
                </HBox>

                <Label text="Lịch sử giao dịch:" style="-fx-font-weight: bold; -fx-font-size: 14px; -fx-padding: 0 0 5 0;"/>

                <TableView fx:id="historyTable" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn fx:id="txnRefColumn" text="Mã GD" prefWidth="150"/>
                        <TableColumn fx:id="txnDateColumn" text="Ngày giờ" prefWidth="150"/>
                        <TableColumn fx:id="txnTypeColumn" text="Loại" prefWidth="100"/>
                        <TableColumn fx:id="txnAmountColumn" text="Số tiền" prefWidth="120" style="-fx-alignment: CENTER-RIGHT;"/>
                        <TableColumn fx:id="txnBalanceAfterColumn" text="Số dư sau" prefWidth="120" style="-fx-alignment: CENTER-RIGHT;"/>
                        <TableColumn fx:id="txnDescColumn" text="Nội dung" prefWidth="250"/>
                    </columns>
                    <columnResizePolicy>
                        <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                    </columnResizePolicy>
                </TableView>
            </VBox>
        </items>
    </SplitPane>

</VBox>
